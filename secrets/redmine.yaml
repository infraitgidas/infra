redmine:
    vm_pass: hlsv.2025
    postgres_db: redmine
    postgres_user: redmine
    postgres_password: changeme  # Encrypt with: sops -e -i secrets/redmine.yaml
    redmine_secret_key: changeme  # Generate with: openssl rand -hex 32
