# Override default nginx config — all traffic handled by redmine.conf
# This file intentionally left minimal to prevent duplicate server blocks.
server {
    listen 80;
    server_name _;
    return 444;
}
