# Variables de entorno requeridas para el Portal GIDAS
# Copiar a .env y completar con los valores reales

# Password para el bind DN de AD (CN=infrait,OU=ServiceAccounts,DC=GDC01,DC=local)
LDAP_BIND_PASSWORD=

# (Opcional) Secret para firmar JWT. Si no se define, se genera automáticamente.
# JWT_SECRET=
